# Binarios de yt-dlp y FFmpeg

No se versionan (son pesados). Instalá con:

```bash
bash scripts/install-music-tools.sh
```

Opcional en `.env`:

```env
MUSIC_YT_DLP_PATH=/ruta/a/yt-dlp
MUSIC_FFMPEG_PATH=/ruta/a/ffmpeg
MUSIC_DOWNLOAD_TIMEOUT=300
```
